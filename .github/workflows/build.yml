name: build

on: [push, pull_request]

jobs:
  build:
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v2
    - name: configure
      run: |
        cd Homework
        mkdir build
        cd build
        cmake ..
    - name: build
      run: cmake --build build
